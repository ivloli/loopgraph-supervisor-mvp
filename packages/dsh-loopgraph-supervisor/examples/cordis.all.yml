- insert:
    - id: dsh-loopgraph-supervisor
      name: dsh-loopgraph-supervisor
      config:
        maxAttempts: 3
        requirePromotionApproval: true
        # Set this only when dsh-workflow is mounted and a saved workflow exists.
        # workflowName: scoped-review
